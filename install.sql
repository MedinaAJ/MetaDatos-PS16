CREATE TABLE IF NOT EXISTS `PREFIX_metadatos` (
  `id_product` int(10) unsigned NOT NULL,
  `url` text,
  PRIMARY KEY (`id_product`)
) ENGINE=ENGINE_TYPE DEFAULT CHARSET=utf8;

INSERT INTO `PREFIX_minicskeleton` VALUES (0, '');